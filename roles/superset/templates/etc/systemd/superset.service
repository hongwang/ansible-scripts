[Unit]
Description=Superset daemon
After=network.target

[Service]
Environment=PYTHONPATH=${PYTHONPATH}:{{ superset_home }}
Type=simple
ExecStart=/usr/local/bin/superset runserver -t 600

[Install]
WantedBy=multi-user.target
